server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # API 流量由 Envoy Gateway 的 Gateway API HTTPRoute 路由。Nginx 只在 Web
  # Deployment 内提供 Vue bundle 和 SPA 回退页面。
  location / {
    try_files $uri $uri/ /index.html;
  }
}
